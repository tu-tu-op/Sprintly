{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Sprintly (Current Root Build)",
      "type": "extensionHost",
      "request": "launch",
      "preLaunchTask": "npm: compile",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "--disable-extensions"
      ],
      "outFiles": []
    }
  ]
}
